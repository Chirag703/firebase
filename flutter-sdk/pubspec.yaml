name: firebase_console_sdk
description: Firebase Console SDK for Flutter - integrates with the Firebase Console Spring Boot backend.
version: 1.0.0
homepage: https://github.com/your-org/firebase-console-sdk

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.1.0
  shared_preferences: ^2.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
